kind: ConfigMap
apiVersion: v1
metadata:
  name: "{{ .Values.microservice.name }}-config"
  namespace: {{ .Values.microservice.namespace }}
data:
    PORT: '8100'
    APP_ENV: 'production'
    DB_HOST: 'my-ip-postgres'
    DB_PORT: '5432'
    DB_NAME: 'my-ip-db'
